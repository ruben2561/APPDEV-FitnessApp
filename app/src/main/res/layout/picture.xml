<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_margin="1dp"
    android:padding="2dp"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/galleryPicture"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/txtDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/midnight_blue"
        android:gravity="center"
        android:text="Date"
        app:layout_constraintBottom_toTopOf="@id/txtWeight"
        app:layout_constraintEnd_toEndOf="@id/galleryPicture"
        app:layout_constraintStart_toStartOf="@id/galleryPicture"
        app:layout_constraintTop_toBottomOf="@+id/galleryPicture" />

    <TextView
        android:id="@+id/txtWeight"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/midnight_blue"
        android:gravity="center"
        android:text="Weight"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/galleryPicture"
        app:layout_constraintStart_toStartOf="@id/galleryPicture"
        app:layout_constraintTop_toBottomOf="@+id/galleryPicture" />
</androidx.constraintlayout.widget.ConstraintLayout>